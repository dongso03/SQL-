RDBMS 

select : 테이블에서 행과 열을 조절하여 조회 할수 있음. (보고싶은 컬럼만 조절하여 볼수 있다.)
From : 테이블 이름
DESC : 테이블의column 구성을 살펴볼수있음.
WHERE : 조건절을 만족하는 행만을 조회함.
order by :행의 순서는 정렬을 통해 정해줄수 있다.
concat() : null 값만 아니면 모든것 결합 가능
